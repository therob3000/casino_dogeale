<% @bets.each do |bet| %>
<div id="bet">
  <p id="bet_title"><a href="/bet/<%=bet.id%>"><%= bet.title %></a> (<%= bet.remainder %>)</p>
  <p id="bet_tags">
    <% bet.tags.each do |tag| %>
      <a href="/tag/<%=tag.name %>"> <%= tag.name %></a>
    <% end %>
  </div>
  <% end %>
